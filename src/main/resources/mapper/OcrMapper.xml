<?xml version="1.0" encoding="UTF8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- JAVA와 연결할 Mapper 파일 설정 -->
<mapper namespace="kopo.poly.mapper.IOcrMapper">

    <!-- 읽은 데이터 저장 쿼리 -->
    <insert id="insertOcr" parameterType="OcrDTO">
        INSERT INTO OCR_INFO
        (
         SEQ, FILE_NAME, FILE_PATH, ORG_FILE_NAME, EXT, OCR_TEXT,
         REG_ID, REG_DT, CHG_ID, CHG_DT
        )
        VALUES(
        (SELECT NVL(MAX(A.SEQ),0)+1 FROM OCR_INFO A),
               #{fileName}, #{filePath}, #{orgFileName}, #{ext}, #{textFromImage}, #{regId}, NOW(), #{chg_Id}, NOW()
              )
    </insert>

</mapper>